# Requirements delta — Effort 043

## Added

- **`CLAUDE.md`** — 5 new rows in "Skills worth reaching for": `ask-matt`, `to-spec` + `to-tickets`,
  `llm-council`, `writing-plans`, `impeccable`.
- **`process.skills`** (`src/content/site.ts`) — 4 new entries: `to-tickets`, `ask-matt`,
  `llm-council`, `writing-plans`. Bubble count on `/process` goes from 11 to 15.
- **`tests/skills-bubbles.spec.ts`** — `EXPECTED_SKILLS` kept in sync with the 4 additions, per the
  file's own header comment that the two arrays must not drift.
- **This effort's own record** (`aidlc-docs/efforts/043-skills-bubble-matt-pocock-pack/`).

## Changed

- **Effort 027's evidence rule** — not amended, explicitly overridden for this one addition by
  direct owner instruction after the conflict was surfaced. Effort 027's own record and audit rows
  are untouched; they still accurately describe why those two names were excluded at the time.

## Removed

None.
