# Approval-gate audit log

Records every approval-gate response in the project's history.

**Honesty note.** Efforts 001-004 predate AI-DLC adoption in this repo. No approval gates existed at the time — there was no planning stage, no construction gate, and no review step; changes were committed directly with messages like "lets see" and "hmmm". Their rows below are **reconstructions of what happened, not records of approvals**, and are marked `n/a — pre-adoption`. No genuine approval was given or recorded for those efforts. Efforts 005 and 006 were gated by GitHub PR review. Effort 007 is the first to run an explicit AI-DLC scope-approval gate.

| Date | Effort | Gate | Response | Notes |
|------|--------|------|----------|-------|
| 2026-01-27 | 001 | Planning | `n/a — pre-adoption` | No approval gate existed at the time. Reconstructed 2026-08-23 from commits `d9e0d8a`..`faf40ff`. No rationale recorded. |
| 2026-01-27 | 001 | Construction | `n/a — pre-adoption` | No gate. Committed direct to default branch; two corrective `deploy.yml` commits followed. |
| 2026-01-28 | 002 | Planning | `n/a — pre-adoption` | No approval gate existed at the time. Reconstructed from the rename/deletion sets in `e3d7eea`..`c91a044`. |
| 2026-01-28 | 002 | Construction | `n/a — pre-adoption` | No gate. The decision to abandon upstream rebase (ADR 0003) was never reviewed at the time. |
| 2026-01-28 | 003 | Planning | `n/a — pre-adoption` | No approval gate existed at the time. Reconstructed from `7eba8fd`..`e6629aa`. |
| 2026-01-28 | 003 | Construction | `n/a — pre-adoption` | No gate. The JSX-in-content trade-off (ADR 0004) was written retroactively, not approved up front. |
| 2026-01-28 | 004 | Planning | `n/a — pre-adoption` | No approval gate existed at the time. Reconstructed from single commit `0814927`. |
| 2026-01-28 | 004 | Construction | `n/a — pre-adoption` | No gate. `public/images/**` became generated output without review. |
| 2026-08-23 | 005 | Planning | GitHub PR review (PR #1) | Scope and rationale stated in the PR #1 body — the first written rationale in this repo's history. |
| 2026-08-23 | 005 | Construction | Approved and merged (PR #1) | Reviewed on GitHub and merged. `npx tsc --noEmit` clean at merge. Commit `6799e4b`. |
| 2026-08-23 | 006 | Planning | GitHub PR review (PR #2) | Scope stated in the PR #2 body. |
| 2026-08-23 | 006 | Construction | Approved and merged (PR #2) | Reviewed on GitHub and merged. `npx tsc --noEmit` clean; dev server compiled `/work` and `/about`. Commit `1cde09f`. |
| 2026-08-23 | 007 | Planning (scope approval) | `Continue` | User selected scope "Docs structure + a live /process page" and depth `standard`. |
| 2026-08-23 | 007 | Construction | Approved and merged (PR #3) | Reviewed on GitHub and merged. `npx tsc --noEmit` clean; `npm run build` succeeded; `/process` verified reachable from nav. Commit `ac2a0a3`. Registry/audit rows for this effort were left `in-progress`/`pending` after merge and were only corrected during effort 009's sync pass on 2026-08-23. |
| 2026-08-23 | 008 | Planning | GitHub PR review (PR #4) | Scope and rationale stated in the PR #4 body. No `aidlc-docs` effort record was created at the time; backfilled during effort 009's sync pass. |
| 2026-08-23 | 008 | Construction | Approved and merged (PR #4) | Reviewed on GitHub and merged. `npx tsc --noEmit` clean; `npm run build` succeeded (30 static pages). Commit `953ac4f`. |
| 2026-08-23 | 009 | Planning | User request (chat) | User asked to fix the RECAP-Net publication link, drop the duplicate Projects card, and move Amazon ML Summer School into Work Experience; scoped and executed directly, no ADR needed. |
| 2026-08-23 | 009 | Construction | Approved and merged (PR #5) | Reviewed on GitHub and merged. `npx tsc --noEmit` clean; `npm run build` succeeded. Commit `1f71772`. During this effort, discovered and corrected stale registry/audit rows for effort 007 and a missing record for effort 008. |
| 2026-08-23 | 010 | Planning | User request (chat) | User asked why AI tools skipped effort recording on PRs #4-#5 and requested enforcement. Root cause: rule was Claude-only, advisory, with an undefined trivial loophole. Scope: shared-contract rule in AGENTS.md + CI gate + PR template + ADR 0009. |
| 2026-08-23 | 010 | Construction | Via GitHub PR review | Guard script self-validated on its own branch (fails without aidlc-docs change, passes with). `npx tsc --noEmit` clean; `npm run build` succeeded. First PR gated by `aidlc-check` and the new PR template. |
| 2026-08-25 | 011 | Planning (product interview) | Answered | impeccable `init` interview run with the structured question tool. User set: research-weighted audience, "break out where it matters" on Once UI, all routes in scope, monochrome base with a working theme toggle, and an explicit improve-and-verify loop. Recorded in `PRODUCT.md`. |
| 2026-08-25 | 011 | Direction (visual world) | Segmentation overlay | Seven grounded directions derived from the audience's visual world; `concept-seed` (seed `5bc9e1e1`) assigned index 4. Full hand presented with challengers and the standing exit; user chose candidate 1, segmentation overlay, over the assignment. |
| 2026-08-25 | 011 | Construction | Via GitHub PR review | `npx tsc --noEmit` clean; `npm run build` succeeds; `detect.mjs --json` returns `[]`; contrast measured AA in both themes. Finish review with screenshots NOT run — screenshot tooling unavailable this session; disclosed in the effort record and ADR 0010. |
| 2026-08-25 | 012 | Planning | User request (chat) | Verified performance defect: `public/images/projects/tinysafetynet.png` at 6.0 MB shipped full-resolution to phones, `grep -c srcset out/index.html` returned 0, and the `sizes` props were inert under `images.unoptimized: true`. Scope: build-time pipeline inside the existing sync step, plus a manifest; component wiring deliberately excluded. |
| 2026-08-25 | 012 | Construction | Via GitHub PR review | `npx tsc --noEmit -p tsconfig.json` clean; `npm run build` succeeds (29 static pages). Pipeline run three times: cold 29.7 s / 142 encodes, warm 0.21 s / 0 encodes, warm-after-full-sync 0.23 s / 0 encodes / 13 cache restores. Quality measured by PSNR against pristine sources: all PNG fallbacks bit-exact, JPEG fallbacks ≥ 38.2 dB, top-rung AVIF 35.5–52.8 dB. |
| 2026-08-25 | 013 | Planning | User request (chat) | Owner asked for a separate branch `extremechange`, the current site removed, and a from-scratch rebuild modelled on thine.com with no obligation to keep static export or existing style. Executed directly on the branch; ADR 0011 records the decisions. |
| 2026-08-25 | 013 | Construction | Via GitHub PR review (pending) | `npx tsc --noEmit -p tsconfig.json` clean; `npm run build` 27 pages; all routes probed; Playwright screenshots of every page reviewed, three defects caught visually and fixed (runtime `animate` offset error, sequence start slice, hero overlap). |
| 2026-08-25 | 014 | Planning | User request (chat) | Owner reported scroll lag on the home page and doubled words when copying the passage, and chose replacement passage copy (#1 of four offered). Performance work delegated to an agent with a measure-first brief. |
| 2026-08-25 | 014 | Construction | Via GitHub PR review (pending) | Production-build frame-time harness: p95 283 → 17.1 ms, frames > 50 ms 70 → 4. Root cause attributed by bisection to the blurred `text-shadow` on scaling cards. `npm run typecheck` clean; `npm run build` succeeds; reduced-motion mode renders with zero console errors. |
| 2026-08-25 | 015 | Planning | User request (chat) | Owner: keep the UI, fix the UX — nobody understands what he does or what World Rank 3 means; /process opaque and should not lead with Optum; no way home from other routes; brain copy overlaps the image. Two agents with disjoint file ownership: impeccable (nav, hero, brain) and UI/UX Pro Max (/process, /story, cards, experience, footer). PRODUCT.md rewritten first. |
| 2026-08-25 | 015 | Construction | Via GitHub PR review (pending) | impeccable critique before: `/` 19/32, `/process` 22/32. After: overlap 398 px → 0; Home pill on every route; every acronym on the first three viewports glossed; Optum removed from /process; stats corrected. `npm run typecheck` clean; `npm run build` succeeds; Playwright rounds at 1440/390 with zero console errors. |
| 2026-08-26 | 016 | Planning | User request (chat, `/frontend-design`) | Owner: keep the current UI, add lexsi.ai's animations where they make it better — add, never remove. Five techniques chosen from the effort-011 teardown; fluid root type, nav change and font swap explicitly not taken. |
| 2026-08-26 | 016 | Construction | Via GitHub PR review (pending) | `npm run typecheck` clean; `npm run build` succeeds; ribbon throttled to 15 fps and paused off-screen; reduced-motion paths verified; also fixed a Motion `useScroll` ref-not-hydrated warning in the reduced-motion branches of CardStack and BrainSequence. |
| 2026-08-26 | 017 | Planning | User request (chat) | Owner: make every project write-up as detailed as AtoM-Net, and only from real sources — "don't make up and write". READMEs of all 18 linked repositories fetched via the GitHub API. |
| 2026-08-26 | 017 | Construction | Via GitHub PR review (pending) | Nine substantive READMEs → full case studies (163–649 words); six one-line READMEs → short honest bodies stating only stack, deploy link and demo video. Frontmatter byte-identical on all fifteen. Boilerplate pasted between repos, private edit links and unverifiable counts deliberately left out. `npm run typecheck` clean; `npm run build` succeeds. |
| 2026-08-26 | 018 | Planning | User request (chat) | Owner chose two items from the improvement list: shared-element morph between project thumbnail and case-study hero; an illustrative tumour outline during the RECAP-Net stage of the brain sequence. |
| 2026-08-26 | 018 | Construction | Via GitHub PR review (pending) | `<ViewTransition share="morph">` on both ends with `default="none"`; 420 ms morph with mid-flight blur; disabled under reduced motion. Canvas outline across frames 58–102, labelled "illustrative · template brain" on the canvas itself. `npm run typecheck` clean; `npm run build` succeeds; browser-verified. |
| 2026-08-26 | 019 | Planning | User request (chat) | Owner: add BrainwavesFinland (private repo — explain, don't publish the pipeline; setup image as banner) and SAAKSHI, the TechGig × Optum hackathon project in the private `InnovationForIndia-Optum` repo, as detailed projects; keep both out of the home selection. |
| 2026-08-26 | 019 | Construction | Via GitHub PR review (pending) | BrainwavesFinland written from the private repo's README/RESULTS via the owner's GitHub token with pipeline specifics generalised; SAAKSHI written from the repo's prototype README, PRODUCT.md, demo output and pitch-site copy; banners = reconstruction montage and a screenshot of the pitch site. An Optum enablement-agent page written by mistake was removed. `npm run typecheck` clean; `npm run build` succeeds. |
| 2026-08-27 | 020 | Planning | User request (chat) | Owner ran Lighthouse on the live `/story` (four runs in `H:\mywebsite\rports\`) and asked to raise scores without lowering another category. Read the four report JSON blobs; accessibility was identically 96 on every run (the only sub-100 category), from two shared-chrome audits: `color-contrast` (`.label` 3.86:1, footer text 3.30:1) and `label-content-name-mismatch` (wordmark aria-label ≠ visible text). Scoped to CSS-colour + one attribute; no JS/markup/route change, so no other category can regress. Closes the AA follow-up deferred in effort 015. |
| 2026-08-27 | 020 | Construction | Via GitHub PR review (pending) | `.label` 45 % → 55 % paper (3.86:1 → 5.14:1); footer colophon/copyright `/40` → `/55` (3.30:1 → 5.14:1); wordmark `aria-label` replaced with a `sr-only ", home"` suffix so the accessible name is a superset of the visible text. `npm run typecheck` clean; `npm run build` succeeds (30 static pages); emitted `out/` grepped to confirm each fix and that every remaining `text-paper/40` is the `aria-hidden` `↗` (contrast-exempt). Performance untouched by design — desktop already 100, mobile 89's remaining levers are GitHub-Pages cache headers (not repo-fixable) and the pinned Motion/Lenis animations. |
| 2026-08-27 | 022 | Planning | User request (chat) | Owner supplied a newer résumé (`H:\mywebsite\nlp resume.pdf`, 151821 bytes) and seven new JPEGs (`H:\mywebsite\new pics\`) to add to the gallery. Confirmed `person.resume` in `site.ts` already points at `/resume.pdf` (no rename); confirmed the new photos' `2026-08-27` filenames sort after the existing eight `2026-01-27` files so `gallery:5`/`gallery:1` in `site.ts` keep resolving to the same two photos after `npm run images` renumbers the drop-zone. |
| 2026-08-27 | 022 | Construction | Via GitHub PR review (pending) | Replaced `resume.pdf` and `public/resume.pdf` (141496 → 151821 bytes); added seven JPEGs to `gallery/`; ran `npm run images` (8 → 15 gallery entries in `src/data/images.json`, indexes 1–8 unchanged). No component changes needed — `Gallery.tsx` already renders any photo count via `columns` masonry with `Photograph N` alt text. `npm run typecheck` clean; `npm run build` succeeds; served `out/` and screenshotted `/` and `/gallery` at 1440×900 and 390×844 with Playwright — all 15 photos render, none sideways, no orphan layout; confirmed via DOM query the home card-stack still resolves to `/img/gallery/05.webp` and `/img/gallery/01.webp`; `curl` confirmed `/resume.pdf` serves 151821 bytes. `npm run test:smoke` skipped — no smoke-test tooling exists in this worktree's commit. |
| 2026-08-27 | 021 | Planning | User request (chat) | Owner asked for an official Lighthouse PR gate and a "nothing breaks" frontend gate, and supplied two new live reports for `/` (a11y 96, agentic-browsing 50 on both form factors). Root cause of both: `aria-label` on `<p>`/`<span>` in the split-text components. Chose Google's `@lhci/cli` and Microsoft's `@playwright/test`, run against the export via `serve`. ADR 0012. |
| 2026-08-27 | 021 | Construction | Via GitHub PR review (pending) | First local Lighthouse run failed and surfaced three more a11y defects (`/process` 45 % mono, `/projects/atomnet` 50 % date, `/projects` h1→h3) and a 2.6 s home LCP that was 95 % render delay on a Motion-animated hero word. Fixed: `sr-only` copies, contrast lifts to 55 %, `headingLevel` prop, CSS-driven hero entrance, `siteUrl` → `.tech`. Final: all 12 route×preset assertions pass; home desktop 0.84 → 0.99, mobile 0.59 → 0.82; a11y 1.0 everywhere. `typecheck` clean; `build` 30 pages; smoke 56/56. |
| 2026-08-28 | 023 | Planning | GitHub spec #12 + tickets #14–#17 | Spec #12 ("factuality evals for AI-written case studies") and its four blocking tickets scoped the work: a tested pure core, a CLI with distinct exit codes, a hand-triaged baseline, and a CI gate plus docs. Inventoried the content first — 20 case studies, 18 with a `link`, two private-source. Chose `node --test` (built into Node 20, no new test framework) and the pure-core seam so the pass/fail logic is provable without network. ADR 0013 records the decisions. |
| 2026-08-28 | 023 | Construction | Via GitHub PR review (pending) | First run over the twenty case studies found 8 ungrounded claims. Triaged by hand: **two real defects fixed in the content** — `Missing-person-identification.mdx` claimed "Smart India Hackathon 2024 … top 45" and `Agentic-Loan-Assistant-Chatbot.mdx` claimed "Built for EY Hackathon 2025", neither present in the README, `resume.pdf`, `site.ts` or anywhere in the repo; five claims baselined with their real source named (`resume.pdf`, `site.ts`, effort 006's record, and two README config literals); four extractor precision defects fixed in the core with tests. Final: 21 grounded, 5 baselined, 0 ungrounded, 41 unverifiable, exit 0. Gate proved to gate on a scratch commit — an invented `97.3 %` turned the run red naming file, claim and source; reverting turned it green. `npm run typecheck` clean; `npm run build` succeeds (30 static pages); `npm run test:unit` 34/34; `npm run check:aidlc` passes. Smoke and Lighthouse not run locally (no rendering code touched, ports held by a parallel effort) — CI's *Quality gates* covers both. |
| 2026-08-28 | 023 | Construction (second pass) | Self-caught on the first CI run | The first `Evals / factuality` run on PR #22 went red — on the harness, not on a claim. `ComPhysGroup/PyAMorph` returns 404 to the workflow's `GITHUB_TOKEN` (which can only read this repository), so `pySdf.mdx`'s claims became `unverifiable`, its baselined `2026` was no longer "used", and the stale-entry check flagged a perfectly good baseline entry. Root cause: staleness was keyed on the entry being *used* rather than on the claim still *existing* in the content, which is what ticket #16 specifies. A private source must not be able to manufacture a factuality failure. Fixed with a regression test naming the incident; an entry whose claim is now grounded is reported as *redundant* (advisory). Also corrected the untrue "every source repository is public" statement in the workflow comment, `sources.mjs`, `AGENTS.md`, `CONTEXT.md`, `evals/README.md` and ADR 0013. |
| 2026-08-28 | 024 | Planning | Spec #13, tickets #18-#21 (GitHub) | Owner's spec: make the site legible and callable for AI agents without changing a pixel for humans. Read the four `agentic-browsing` audits' source in `lighthouse@13.4.1` before writing any code: `llms-txt` scores **0** for a 2xx file with no H1, no Markdown link or under 50 chars (a 4xx is `notApplicable`), and all three `webmcp-*` audits short-circuit to `notApplicable` unless the gatherer finds both `navigator.modelContext` and the `WebMCP.enable` CDP domain. That fixed the risk model: the llms.txt half is the only half that can lower the score, and only by being malformed. Also established that the repo's pinned gate (`@lhci/cli@0.15.1` -> `lighthouse@12.6.1`) predates the category entirely, so the before/after had to be measured with an ad-hoc `lighthouse@13.4.1` run. ADR 0014. |
| 2026-08-28 | 024 | Construction | Via GitHub PR review (pending) | Pure renderer `scripts/lib/llms-txt.mjs` (no I/O, 8 `node --test` cases) + `scripts/build-llms-txt.mjs` on `predev`/`prebuild`, writing two gitignored files into `public/`; `src/content/site.ts` loaded by in-memory transpile so there is no second copy of the copy. WebMCP: one capability-checked `searchProjects` tool on `/projects` only, rendering `null`, unregistering on unmount, driven by the same list the page renders; verified with a `navigator.modelContext` stub injected via `addInitScript`. `npm run typecheck` clean; `npm run build` succeeds; `npm run test:unit` 8/8; `npm run test:smoke` green; both Lighthouse presets green. Agentic-browsing held at 1.00 on all 12 route x preset runs with `llms-txt` moving `notApplicable` -> **pass**; the three `webmcp-*` audits stayed `notApplicable` because no browser implements the API. |
| 2026-08-31 | 025 | Planning | User request (chat) | Issue #26 asked for professional docs; PR #29 (opened by another AI session against the same issue) was reviewed and closed for deleting concrete evidence (commit hashes, exact messages, line counts) in favour of vague paraphrase, deleting the "Run locally" section, and asserting a false "3D/glassmorphism" design description. Redone from scratch, keeping evidence. |
| 2026-08-31 | 025 | Construction | Via GitHub PR review (pending) | README gains CI badges, a hook paragraph naming the four PR gates, a corrected project count (18→20, a real staleness bug), and a completed `Build & deploy` command list. `npm run typecheck` clean; `npm run build` succeeds. |
| 2026-08-31 | 026 | Planning | User request (chat, issue #25) | Owner reported that after adding Cloudflare, an AI assistant could fetch only the home page instead of the whole site. Live `robots.txt` fetch found the cause: Cloudflare's edge-injected default block sets `Disallow: /` for nine AI/search bots including ClaudeBot, GPTBot and Google-Extended, contradicting ADR 0014's purpose. |
| 2026-08-31 | 026 | Construction | Via GitHub PR review (pending) | ADR 0015 records the policy (deny training, allow retrieval) and the Cloudflare dashboard action needed to apply it. No code change: the restriction is at the edge, not in `src/app/robots.ts`. `npm run typecheck` clean; `npm run build` succeeds; `check:aidlc` OK. |
| 2026-09-01 | 027 | Planning | GitHub issue #27 (spec via `/to-spec`) | Owner: add a bubble cluster to `/process` naming every Claude Skill actually used to build the site — names only, never a skill's content. Grounded the list two ways: `CLAUDE.md`'s "Skills worth reaching for" table, and a grep of `aidlc-docs/efforts/*/effort-state.md`, `aidlc-docs/audit.md`, `docs/adr/*.md` and GitHub issue bodies for skills cited by name. Dropped four names from the issue's own seed list (`subagent-driven-development`, `to-tickets`, `triage`, `ask-matt`) for having zero occurrences anywhere in the repo. Final 11: `ai-dlc`, `brainstorming`, `agent-swarm`, `dispatching-parallel-agents`, `frontend-design`, `documentation-bot`, `code-review`, `verification-before-completion`, `impeccable`, `ui-ux-pro-max`, `to-spec`. |
| 2026-09-01 | 027 | Construction | Via GitHub PR review (pending) | New `process.skills`/`skillsLabel`/`skillsNote` in `src/content/site.ts`; new `src/components/process/SkillsBubbles.tsx` (golden-angle bubble layout, idle drift + click/drag spring nudge via Motion's `useAnimationControls`, `useReducedMotionSafe()` fallback to a plain `<ul>` list of the same names); one new `<Reveal>` section appended to `/process`, nothing else on the page moved. `npm run typecheck` clean; `npm run build` succeeds (30 static pages); new `tests/skills-bubbles.spec.ts` (every name present as DOM text, a click changes a bubble's `transform`, a drag moves its bounding box >20px, reduced motion renders the static list with zero console errors) run against a temporary local Playwright config on port 3417 (not 3100/3200/3201, reserved for concurrent agents) — 4/4 passed; full existing suite re-run on the same port, 36/36 passed, no regression. `npm run check:aidlc` passes. |
| 2026-09-01 | 028 | Planning | Issue #28 debate synthesis (owner comment) | Issue #28 ran the `agent-swarm` debate pattern by design: two independent prototypes (`prototype/story-tech-revival-a2` hover-lean/tap-nudge plain text above the static Tools list; `prototype/story-tech-revival-b2` click/tap-to-reshuffle pills below it) both duplicated the existing static Tools list rather than resolving it. The synthesis comment on the issue picked B's interaction and directed this effort to replace the static Tools pills in place rather than add a second section — that decision is carried into this effort's record, not re-litigated. |
| 2026-09-01 | 028 | Construction | Via GitHub PR review (pending) | `src/components/story/ToolkitToy.tsx` adapts proposal B's component: click/tap reshuffles via Fisher–Yates, Motion `layout` spring reflow, `useReducedMotionSafe` renders the identical static list. `src/app/story/page.tsx`'s Tools `<ul>` replaced in place with `<ToolkitToy tools={story.skills} />`; nothing else on the page touched. `npm run typecheck` clean; `npm run build` succeeds (30 static pages, `out/story/index.html` present). `tests/smoke.spec.ts` extended with a `story Tools toy` block (DOM-text presence, measurable click-driven reorder, reduced-motion static fallback with zero console errors) — 3/3 pass; full smoke suite 30/31, the one failure a reproduced-flaky local timeout on `renders / without errors` unrelated to `/story`. |
| 2026-09-01 | 029 | Planning | User request (chat) | Owner: README still quoted the informal early commit messages verbatim (unprofessional on a first-impression doc), the tech-stack line read as too plain, and the four PR gates deserved an explicit write-up. Scoped to README.md only, per the owner's own wording — the same quotes stay in the AI-DLC evidence trail (ADR 0008, registry, audit, effort records), a different document with a different job. |
| 2026-09-01 | 029 | Construction | Via GitHub PR review (pending) | Informal quotes replaced with commit hashes and the 12,517-line figure (claim stays checkable, literal words gone); flat stack line replaced with a Layer/Technology table; new Quality gates table (# / Gate / Workflow / Checks / Fails when) for all four PR gates. `npm run typecheck` clean; `npm run build` succeeds. |
| 2026-09-01 | 030 | Planning | User request (chat) + brainstorming skill | Owner: skills bubble at the bottom catches no attention, CI gates need a diagram not prose, surface what's genuinely different about this site. Brainstorming skill ran; two structured questions (placement: after stats; diagram: horizontal pipeline). Copydesk installed with a voice register bootstrapped from the owner's session messages. |
| 2026-09-01 | 030 | Construction | Via GitHub PR review (pending) | New section order on /process (skills moved to position 3, gate pipeline at 4, "what's different" at 7). New `GatePipeline` component (pure CSS, responsive horizontal/vertical, tangerine nodes). Five "what's different" items in site.ts with evidence links. Headline shortened, Verify step abbreviated, counts updated to 29/15. `npm run typecheck` clean; `npm run build` succeeds. |
| 2026-09-01 | 031 | Planning | User request (chat) | Owner asked for site improvement ideas; brainstorming skill run. 14 ideas documented in `docs/plans/2026-09-01-site-improvement-ideas.md`, ordered by impact-per-effort. |
| 2026-09-01 | 031 | Construction | Via GitHub PR review (PR #43) | Ideas doc written with what/why/how/cost/risk for each. No code change — docs only. |
| 2026-09-02 | 032 | Planning | User request (chat) | Owner: implement best ideas from PR #43 plus OG image for WhatsApp/Telegram. Chose ideas 11 (interest pills), 12 (kill duplicate text), 13 (education timeline), 7 (real sitemap lastmod), and OG verification. |
| 2026-09-02 | 032 | Construction | Via GitHub PR review (pending) | Interests as glass pills with accent dots; education as vertical timeline with tangerine dots; duplicate AIR-14/top-30 removed from `story.more[1]`; sitemap uses project dates instead of `new Date()`; OG image verified correct (1200×630 JPEG, absolute URL, width/height meta). `npm run typecheck` clean; `npm run build` 30 pages; `npm run test:smoke` 77/78 (1 pre-existing flake). |
| 2026-09-02 | 033 | Planning | User request (chat) | Owner updated six repositories' READMEs in a prior session; asked to refresh the matching case studies and close issue #41 ("remove footer"). Re-fetched all 20 project READMEs; found six had grown from single-line placeholders (326–1662 words) far past their 37–93-word case studies: `gui-CANSAT`, `alien_invasion_pygame`, `Object_tracking_tennis_game`, `youtubeproj_langchain`, `Tomato_disease_detection`, `Anime_Recommender`. |
| 2026-09-02 | 033 | Construction | Via GitHub PR review (pending) | All six case studies rewritten from their current READMEs, following the evidence rule. `youtubeproj-langchain.mdx`'s source repo was rewritten entirely into "YouTube Comment RAG" (consensus-weighted retrieval); case study and title rewritten to match. Issue #41: full footer-colophon removal would violate the ICBM 152 template licence requirement already documented in `AGENTS.md`; resolved by removing the two decorative lines (font credit, "built with" blurb) and keeping only the required licence notice. `npm run typecheck` clean; `node evals/factuality/run.mjs` 60 grounded / 9 baselined (one new entry, same code-span pattern as existing tinySafetyNet entries) / 0 ungrounded, exit 0; `node --test evals scripts` 42/42; `npm run build` 30 pages; `npx playwright test` 76/78 (2 confirmed-flaky "Target crashed" resource errors under full-suite load, both pass in isolation). Fixes #41. |
| 2026-09-03 | 038 | Planning | User request (chat) — implement ideation doc effort 031 idea 5 | Owner: finish `/writing` route for the three orphaned `content/writing/*.mdx` posts (idea 5, `docs/plans/2026-09-01-site-improvement-ideas.md`, effort 031). A prior session was interrupted mid-task: `src/app/writing/page.tsx` and `src/lib/writing.ts` existed uncommitted but the list page referenced a nonexistent `writing` export in `site.ts`, `[slug]/page.tsx` and `nav` entry were missing, and no sitemap/Lighthouse/llms.txt/docs/AI-DLC wiring had been done. Reviewed the inherited files against `src/lib/projects.ts` — pattern was correct, no rewrite needed — and scoped the rest: the `[slug]` page (copy `/projects/[slug]`), the `nav`/`writing` exports, per-post sitemap `lastmod` (effort 032's pattern), both Lighthouse route lists, and a decision on `llms.txt` and the factuality gate. Read all three MDX posts in full before touching anything else. |
| 2026-09-03 | 038 | Construction | Via GitHub PR review (pending) | New `src/app/writing/[slug]/page.tsx` mirroring `/projects/[slug]` (no image handling — posts carry an optional `tag`, not `images[]`/`link`); `writing` export added to `site.ts`; `{ label: "Writing", href: "/writing" }` added to `nav`; `sitemap.ts` gained a `writingPages` block keyed on each post's own `publishedAt`; `/writing` added to both `lighthouserc.*.json` route lists. Decided writing posts stay outside the factuality gate (two of three posts have no single checkable source; ADR 0016) but do get an `llms.txt`/`llms-full.txt` `## Writing` section — `renderLlmsTxt(site, projects, posts)` grew an optional third parameter (default `[]`, no existing call site broke) plus 6 new/extended unit tests. Reading all three posts surfaced one real defect (`sehat-sathee-chatbot-blog.mdx`'s inherited draft cut off mid-Dockerfile, unclosed code fence) and two unsourced/likely-outdated claims (a rural-population/internet-access statistic; a CoWIN government API integration against an endpoint restricted before the post's dated hackathon) — consolidated into one dated editor's note, fence closed, nothing invented to fill the gap; full list in the effort record and PR body. `npm run typecheck` clean; `npm run build` succeeds (`out/writing.html` + all three `out/writing/<slug>.html` present, matching the site's existing `out/<route>.html` export convention); `npm run test:unit` 46/46 (6 new); `npm run test:smoke` on the repo's own port 3100 first showed 25 failures, all 404s traced to `reuseExistingServer` reusing a sibling agent's concurrent `serve` process on that port rather than this worktree's own; rerun against a scratch port (3450, not 3100/3200/3201 — the same reason effort 027 used 3417; config deleted after the run, never committed) — 86/86 passed, desktop + mobile, including all three new `/writing/[slug]` pages. |
